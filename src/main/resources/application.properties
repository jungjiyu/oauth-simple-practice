spring.application.name=oauth2_01

# H2 database ->
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#JWT
    # secret key - above 512 bit (HS512)
jwt.secretKey=sdfgae3vdfawegtyvaw43tsdgfsdfedsfsedffedsdfsddsfsdffsdzfsdgvdrtfhfbrtghrtdfg

    # Access Token- 1hour (1000ms * 60s * 60m)
jwt.access.expiration=3600000
jwt.access.header=Authorization

    # Refresh Token - 2weeks (1000ms * 60s * 60m * 24h * 14)
jwt.refresh.expiration=1209600000
jwt.refresh.header=X-Refresh-Token

